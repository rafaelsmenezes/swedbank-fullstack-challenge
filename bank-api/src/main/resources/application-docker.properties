spring.datasource.url=jdbc:postgresql://postgres:5432/bankdb
spring.datasource.username=swedbank
spring.datasource.password=swedbank123
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=false

# CORS - read from env var CORS_ALLOWED_ORIGINS (set in Railway), fallback to localhost
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}
